# 2026-04-27 — Linux Ops live wiring + line clipping fix

## Completed

### Linux Ops Command Copilot wired live (`index.html`)
- Node element (desktop): `data-status="inbuild"` → `"live"`, badge text "In Build" → "Live", aria-label updated.
- Mobile list item: `data-status="inbuild"` → `"live"`, status text "In Build" → "Live".
- Projects data entry: `status: 'inbuild'` → `'live'`, `demoUrl: null` → `'https://d1dqp0w50lre0j.cloudfront.net'`, `archModalId: null` → `'linux-modal'`.
- New `linux-modal` block inserted between FieldIQ modal close and `<script>` tag. Six sections matching FieldIQ pattern: What It Is / Tech Stack / Safety Layer / Key Decisions / Structure / What I Learned. Content sourced from the Linux Ops project's `PROJECT_MASTER.md`, `Bedrock + Lambda + Claude Latency Optimization Report.md`, `Linux_Ops_Command_Copilot_Planning.md`, and `README.md` — every fact traceable to a section/entry in those four docs. TypeScript version verified against `package.json` (`~6.0.2`) before write.
- Side effects (automatic, no JS or CSS changes needed): connection line from hub redraws green via `drawLines()`'s status-based color logic; node card border/background/dot/badge switch to live styling via existing `[data-status="live"]` CSS selectors; detail pane renders Live Demo + Architecture buttons via existing `renderDetail()` `actionsHtml` branch.

### `drawLines()` line/AABB clipping (`index.html`)
- Endpoint computation in `drawLines()` updated to clip each line at the smallest `t ∈ (0, 1]` where the parametric line `p(t) = hub + t*(cardCenter - hub)` crosses the card's bounding rectangle.
- Hub origin logic unchanged. Color/opacity/stroke-width logic unchanged.
- Fixes the line-through-card issue with multi-line labels (Linux Ops, two-line label, taller card pulled the geometric center deeper). Now uniform across all node sizes — short and tall cards behave the same way.
- Falls back to card center (`bestT = 1`) on math degeneracy. Zero regression risk.

### Docs
- `CLAUDE.md`: "Last updated" 2026-04-20 → 2026-04-27. Page Architecture date updated. Connection-line description expanded to document the line/AABB clipping. Architecture-modals heading: "(RAG + FieldIQ)" → "(RAG + FieldIQ + Linux Ops)".
- `Advanced_Projects_Page_Design_Brief.md`: Linux Ops project entry — Status "In Build" → "Live", Buttons "none (In Build — V1)" → "Live Demo + Architecture".

## Skipped / not done
- No CSS changes. Existing `[data-status="live"]` selectors handle every visual transition.
- No source changes to the Linux Ops Command Copilot repo (separate project).

## Errors hit
None.

## Exact next steps
- Visual regression check post-deploy: confirm all five connection lines (RAG, FieldIQ, Linux Ops, AI Security, Cloud Incident) terminate cleanly at their respective card edges with no line-through-card artifacts.
- No specific next session locked.
